#!/usr/bin/python3


def update_dictionary(my_dict, key, value):
    """
    update/create a key/value pair in a dictionary

    my_dict: a dictionary
    key: a key
    value: a value

    Return: the dictionary
    """
    my_dict[key] = value
    return (my_dict)
